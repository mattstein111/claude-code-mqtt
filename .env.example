# MQTT broker connection
MQTT_BROKER_URL=mqtt://192.168.1.197:1883
# MQTT_USERNAME=
# MQTT_PASSWORD=

# Session name — passed at launch: SESSION_NAME=my-session claude --channels ...
# SESSION_NAME=primary

# QoS level (0=at most once, 1=at least once, 2=exactly once)
QOS=1
